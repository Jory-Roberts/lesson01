"use strict";
// Literal Types
let myName = 'Jory';
let userName;
userName = 'Amber';
//Functions
const add = (a, b) => a + b;
const logMsg = (message) => console.log(message);
logMsg('Hello');
logMsg(add(2, 3));
logMsg(add(2, 5));
let subtract = function (c, d) {
    return c - d;
};
